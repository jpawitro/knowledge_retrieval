# Copy this file to .env and fill in the values you need.
# .env is gitignored - never commit real credentials.

# Required only for `knowledge-retrieval-web --engine firecrawl`.
# Get a key at https://firecrawl.dev
FIRECRAWL_API_KEY=
